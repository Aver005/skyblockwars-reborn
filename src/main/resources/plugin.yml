name: SkyBlockWars
main: ru.kiviuly.skyblockwars.SkyBlockWarsPlugin
version: '${version}'
api-version: '1.21'
author: kiviuly
description: SkyBlockWars Reborn - block-epoch survival arena minigame.
depend: [MgCore]
# Своих команд нет: арены/вход — через /mg (ядро), игро-подкоманды (setcenter/setradius/
# epochs/kit/…) — через MgCore-делегирование в SkyBlockWarsGame.onCommand. Права — mg.admin.
